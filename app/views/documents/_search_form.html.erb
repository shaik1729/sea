<%= form_with url: search_documents_path, local: true, method: :get do  |f| %>
    <%= text_field_tag :id, params[:id], :placeholder => "Enter Document ID", class: "form-control me-2" %>
    <p>OR</p>
    <%= text_field_tag :title, params[:title], :placeholder => "Enter Document Title", class: "form-control me-2" %>
    <div class="actions">
        <%= f.submit "Search" %>
    </div>
<% end %>